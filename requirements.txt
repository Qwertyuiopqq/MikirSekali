# requirements.txt - LOCAL environment only
# Training runs in Google Colab. This file covers: pipeline (spine/enrichment/
# xgboost scoring/fuzzy health), local finBERT/IndoBERT inference, data
# scraping, and EDA notebooks. Confirmed against actual imports in codes.zip.

# --- DATA INGESTION & PIPELINE (spine_builder, enrichment, xgboost_predictor, pipeline.py) ---
pandas==2.2.3
numpy==1.26.4
requests==2.34.2          # data-prep/data_scraping.ipynb

# --- MACHINE LEARNING (xgboost_predictor.py - loads pre-trained models, no training) ---
xgboost==2.0.3

# --- EDA (data-analizing/*.ipynb) ---
matplotlib==3.8.4
seaborn==0.13.2
plotly==7.1.0              # NOTE: major bump from 5.x -> 7.x, satisfies >=5.15.0 but check for breaking API changes

# --- NLP / IndoBERT-FinBERT sentiment inference (test/finBERT.py) ---
torch==2.3.1
transformers==4.45.2       # NOTE: original cap was <4.45.0 - widened to match what's actually installed

# --- BACKEND SERVER ---
# Deleted for now - no backend code found in codes.zip. Add back when you build it.